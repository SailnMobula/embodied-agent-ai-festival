---
order: 5
title: The agent
tagline: A prompt, a voice and a list of skills, assembled live.
durationMinutes: 20
takeaway: The same task, without a single branch written in advance. That is the whole difference.
---

import AgentPipeline from '@/components/AgentPipeline.astro'
import StateMachineDemo from '@/components/StateMachineDemo.tsx'

## What we plug together

- **Prompt**: who the robot is, where it stands, what it must never do
- **Skills**: the list from Station 4, handed over as functions
- **Voice**: the model listens and answers continuously

## The path a sentence takes

<AgentPipeline />

Nowhere in there is a list of situations. The model decides.

> **Live demo: build the agent on stage.** Write the prompt, tick three skills, activate, talk to
> it.

## The same task, the old way

<StateMachineDemo client:visible />

Now ask for one more thing, like greeting people by name. On the agent side that is a sentence. On
this side it is a redesign.

## What still does not work

- It calls skills in situations they were never built for
- It cannot do anything no skill exposes
- The same situation twice can end differently
